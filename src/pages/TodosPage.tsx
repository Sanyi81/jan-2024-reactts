import React from 'react';
import TodosComponent from "../components/TodosComponents/TodosComponent";

const TodosPage = () => {
    return (
        <div>
            <TodosComponent todos={[]}/>
        </div>
    );
};

export default TodosPage;